This is a project to test the viability of a single-purpose neural-network-model for achieving complex graphical effects in web-based games.

Setup:

1. install git lfs

2. install dependacies
`$ npm install`

3. run porject
`$ npm run dev`
